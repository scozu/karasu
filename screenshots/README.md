# Karasu Screenshot Slots

Drop screenshots into this folder using these exact PNG filenames:

- `neovim-night.png`
- `neovim-snow.png`
- `zed-night.png`
- `zed-snow.png`

The root README renders these files directly. To update screenshots, overwrite the files with new images using the same names.

Shared editor sample file for captures:

- `screenshots/sample-code/showcase.ts`
